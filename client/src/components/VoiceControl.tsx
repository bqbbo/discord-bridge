import Button from "./Button";

const VoiceControl = () => {
    return (
        <Button
            text="Voice Control"
            onClick={() => console.log("Voice Control Clicked")}
        />
    );
};

export default VoiceControl;
