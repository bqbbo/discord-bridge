import "../styles/ServerSelect.css";

const ServerSelect = () => {
    return (
        <div className="server-select">
            <p>Server Select</p>
        </div>
    );
};

export default ServerSelect;
